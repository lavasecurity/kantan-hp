# One-time setup for Decap CMS GitHub login on Cloudflare Pages.
#
# Run this once per fork after you have:
#   1. Created a GitHub OAuth App (Settings -> Developer settings -> OAuth Apps)
#      with Authorization callback URL: https://<your-site>.pages.dev/api/callback
#   2. Added the Cloudflare secrets below to this repository (Settings -> Secrets
#      and variables -> Actions):
#        CF_API_TOKEN   - a Cloudflare API token with "Cloudflare Pages: Edit" permission
#        CF_ACCOUNT_ID  - your Cloudflare account id
#
# Then go to Actions -> "Setup Decap CMS login" -> Run workflow, and paste in your
# GitHub OAuth App's client ID and secret. The workflow writes them into the
# Cloudflare Pages project as environment variables and redeploys the site.

name: Setup Decap CMS login

on:
  workflow_dispatch:
    inputs:
      client_id:
        description: GitHub OAuth App Client ID
        required: true
        type: string
      client_secret:
        description: GitHub OAuth App Client Secret
        required: true
        type: string
      project_name:
        description: Your Cloudflare Pages project name (from your *.pages.dev URL)
        required: true
        type: string
      deploy_hook:
        description: >
          Optional — Cloudflare Pages deploy hook URL (Settings -> Builds -> Add
          deploy hook). Leave empty to skip the automatic redeploy; the env vars
          will take effect on your next git push.
        required: false
        type: string

jobs:
  set-cloudflare-env:
    name: Set Cloudflare Pages env vars
    runs-on: ubuntu-latest
    steps:
      - name: Set OAuth credentials on Cloudflare Pages
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          PROJECT_NAME: ${{ github.event.inputs.project_name }}
          CLIENT_ID: ${{ github.event.inputs.client_id }}
          CLIENT_SECRET: ${{ github.event.inputs.client_secret }}
        run: |
          set -euo pipefail
          if [ -z "$CF_API_TOKEN" ] || [ -z "$CF_ACCOUNT_ID" ]; then
            echo "::error::Missing CF_API_TOKEN or CF_ACCOUNT_ID secrets. Add them in Settings -> Secrets and variables -> Actions, then re-run this workflow."
            exit 1
          fi

          base="https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/pages/projects/${PROJECT_NAME}"

          # Read the project first so we PRESERVE any existing production env vars
          # instead of replacing the whole map (PATCH replaces it).
          existing=$(
            curl -fsS "$base" -H "Authorization: Bearer ${CF_API_TOKEN}" \
              | jq -c '.result.deployment_configs.production.env_vars // {}'
          )

          # Merge our two OAuth entries into the existing map, then PATCH.
          body=$(jq -cn \
            --argjson existing "$existing" \
            --arg id "$CLIENT_ID" \
            --arg secret "$CLIENT_SECRET" \
            '{deployment_configs: {production: {env_vars:
              ($existing + {
                 OAUTH_GITHUB_CLIENT_ID: {type: "plain_text", value: $id},
                 OAUTH_GITHUB_CLIENT_SECRET: {type: "secret_text", value: $secret}
              })
            }}}')

          curl -fsS -X PATCH "$base" \
            -H "Authorization: Bearer ${CF_API_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$body" \
            | jq -e '.success == true' >/dev/null || {
              echo "::error::Cloudflare API rejected the request. Check CF_API_TOKEN permissions (needs Cloudflare Pages: Edit) and the project name."
              exit 1
            }

          echo "✅ Env vars set (existing project env vars preserved)."

      - name: Redeploy via deploy hook (optional)
        if: ${{ github.event.inputs.deploy_hook != '' }}
        env:
          DEPLOY_HOOK: ${{ github.event.inputs.deploy_hook }}
        run: |
          set -euo pipefail
          # Deploy hooks trigger a rebuild of the Git-connected Pages project
          # without needing a git push. (The direct-upload deployments endpoint
          # is NOT for Git-connected projects.)
          curl -fsS -X POST "$DEPLOY_HOOK" \
            -H "Content-Type: application/json" \
            -d '{}' >/dev/null
          echo "✅ Redeploy triggered. Env vars will apply to the new build."

      - name: Done
        run: |
          echo "Open https://${{ github.event.inputs.project_name }}.pages.dev/admin and log in with GitHub."
          echo "If you skipped the deploy hook, the env vars take effect after your next git push / deploy."
